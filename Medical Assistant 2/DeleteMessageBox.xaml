<Window x:Class="MedicalAssistantCommunity.DeleteMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Delete" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="20" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="-90" Opacity="0.15" ShadowDepth="5"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Icon -->
                <RowDefinition Height="*"/>
                <!-- Text -->
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. Red Delete Icon -->
            <Path Data="{StaticResource DeleteIconPath}" 
                  Fill="#E53E3E" Stretch="Uniform" 
                  Height="40" Width="40" HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <!-- 2. Message Text -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock x:Name="MessageText" 
                           Text="ARE YOU SURE YOU WANT TO DELETE THIS DIAGNOSIS?" 
                           TextWrapping="Wrap" HorizontalAlignment="Center"
                           TextAlignment="Center" FontSize="14" FontWeight="Bold"
                           Foreground="{StaticResource TextDark}"/>
                <TextBlock Name="lBL_Subtitle" Text="This action cannot be undone." 
                           HorizontalAlignment="Center" FontSize="12" 
                           Foreground="{StaticResource TextGrey}" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- 3. Dual Buttons -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Cancel Button -->
                <Button Name="BTN_Cancel" Content="Cancel" Click="Cancel_Click" 
                        Background="Transparent" Foreground="{StaticResource TextGrey}"
                        FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>

                <!-- Delete Button -->
                <Button Name="BTN_Delete" Grid.Column="1" Content="Delete" Click="Delete_Click" 
                        Height="40" Background="#E53E3E" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>